import { Navigate } from 'react-router-dom';
// TODO Person 1: read token and role from localStorage, redirect if unauthorized
export const PrivateRoute = ({ children, role }: { children: JSX.Element; role: string }) => {
  // TODO
  return children;
};
