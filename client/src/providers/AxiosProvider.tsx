import { ReactNode } from 'react';
// TODO Person 1: configure axios instance with VITE_API_URL and JWT interceptor
export const AxiosProvider = ({ children }: { children: ReactNode }) => {
  // TODO
  return <>{children}</>;
};
